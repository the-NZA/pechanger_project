export default {
	// BaseURL: process.env.NODE_ENV === "development" ? "http://localhost:9090"
	// 						: "http://localhost"
	BaseURL: process.env.NODE_ENV === "development" ? "http://localhost:9090"
		: "https://pechanger.ru"
}
